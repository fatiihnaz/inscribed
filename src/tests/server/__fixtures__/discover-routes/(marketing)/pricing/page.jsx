// Route group: "(marketing)" organizes files without appearing in the URL.
export default function Pricing() {
  return <EditableRegion blockPath="pricing.title" blockType="LongText" defaultValue="p" />;
}
